import WirePlaceScene, {
  serializeDiff,
  deserializeDiff,
} from './WirePlaceScene';

export type {
  Actor,
  Update,
  Diff,
  WirePlaceSceneSerialized,
} from './WirePlaceScene';

export { WirePlaceScene, deserializeDiff, serializeDiff };
